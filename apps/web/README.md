# SmartEnergy — Frontend

Next.js dashboard for the SmartEnergy platform.

See the [root README](../../README.md) for setup and deployment.

```bash
npm install
npm run dev
```

Runs at http://localhost:3000 — requires the API on port 8001.
